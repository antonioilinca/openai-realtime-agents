// Configuration Next.js 14 pour l'application LexaIA.
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    serverActions: false
  }
};

module.exports = nextConfig;
